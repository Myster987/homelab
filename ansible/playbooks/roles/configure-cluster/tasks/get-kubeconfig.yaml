- name: Get cluster kubeconfig
  ansible.builtin.command: talosctl kubeconfig ../{{ project_root }}/kubeconfig --talosconfig {{ config_file }}
  changed_when: true
  register: install_output
  tags:
      - get-kubeconfig
