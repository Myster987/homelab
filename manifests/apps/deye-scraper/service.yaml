apiVersion: v1
kind: Service
metadata:
  name: deye-scraper
  namespace: monitoring
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "9100"
spec:
  selector:
    app: deye-scraper
  ports:
    - name: metrics
      port: 9100
      targetPort: 9100
