apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
    name: spegel
    namespace: flux-system
spec:
    interval: 30m
    path: ./manifests/infrastructure/spegel
    prune: true
    sourceRef:
        kind: GitRepository
        name: flux-system
