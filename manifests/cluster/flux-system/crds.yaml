apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: crds
  namespace: flux-system
spec:
  interval: 1h
  path: ./manifests/infrastructure/crds
  prune: false
  wait: true
  sourceRef:
    kind: GitRepository
    name: flux-system
